# 2026-08-10 四项历史回归修复记录

## 验收状态

- 当前状态：**Agent 自测通过，等待用户人工验收**。
- 本记录不将任何项目标记为“人工验收通过”；该结论只能由用户确认。
- 分支：`fix/0810-four-regressions`。

## 需求与根因

| 项目 | 根因 | 修复 |
| --- | --- | --- |
| 同名科目与笔记数不一致 | 既有同名科目在卡片上不可区分；前端把全局 `store.notes` 的陈旧计数与 API 计数取最大值，可能把其他时间点的数据保留下来 | 新建或真正重命名为同名科目时，API 按 `名称-1`、`名称-2` 选择可用名称；仅编辑其他字段不会改动既有同名记录；既有同名数据不改名，显示名还会避开真实的带后缀名称；卡片以 `/api/subjects` 的实时计数为唯一数据源 |
| 策略偏好不能选择真实 Skill | `f765797` 曾实现已安装 Skill 搜索选择，后续被自由关键词输入框覆盖 | 恢复等价行为：页面加载已安装 Skills，按名称、描述、分类搜索，只允许点击搜索结果加入策略 |
| 后台生成偶发“LLM API 返回了空内容” | Chat Completions 只接受字符串 `message.content`，且 Provider 第一次返回空正文时立即失败 | 兼容字符串、结构化文本数组、`choice.text` 与 `output_text`；仅对 HTTP 成功但正文为空的响应自动重试一次，保持 token 预算并降低温度；两次均空时仍明确报错 |
| 环境变量下拉显示不全 | `f765797` 的原生 `select` 后续退回 `datalist`，浏览器不会稳定展示全部候选项 | 恢复原生下拉，完整展示后端返回列表并保留手动变量名入口；额外拆分“启用导入”和“变量名”状态，避免选择手动输入后控件折叠 |

## 历史依据

- `f765797 fix(ui): 修复配置技能与内容编辑体验`：Skill 搜索选择器和环境变量原生下拉的已知良好实现。
- `725293b fix(subject-note-count)`、`85835da fix(subjects): 保存笔记后刷新科目计数`：科目计数历史修复。
- 未整体回滚任何历史提交，只迁移并补强所需行为。

## 数据保护

- 未删除、重建或重命名任何现有科目、笔记、产品或 Skill。
- “赚钱”和四个“自媒体变现方法论”科目均保留。
- 既有同名科目的 `-1/-2/-3` 仅为前端显示名；数据库原值未改变。
- 浏览器验收中的 Skill 和自定义环境变量选择均未点击保存，不产生用户配置变更。

## 自动化证据

- 红灯阶段：同名科目连续创建测试返回三个相同名称；LLM 空正文立即抛错；结构化正文返回数组而非字符串；三个前端源码回归断言失败。
- 绿灯阶段：`python -m pytest -q backend/tests`，350 项通过。
- `npm test -- --run`，14 项通过；其中显示名为可执行纯函数测试，不是源码字符串断言。
- `npm run build`，生产构建成功；仅保留既有的大包体积警告。
- 本轮涉及文件的 `git diff --check` 通过。

## 真实环境证据

- 一键启动脚本重新启动后，`http://127.0.0.1:9000/api/health` 返回 200。
- 真实登录后共有 41 个科目、59 篇笔记；未新增或删除科目。
- 四个“自媒体变现方法论”显示为基础名及 `-1/-2/-3`；抽查 ID 8 和 ID 43，卡片/API/进入后的列表分别均为 14/14/非空和 1/1/非空。
- 策略偏好输入 `ppt` 返回已安装的 `pptx-generator`、`guizang-ppt-skill`，点击后加入真实 Skill 名称且搜索框清空。
- LLM 设置下拉显示后端 17 个环境变量和一个“手动输入其他变量名…”选项；选择手动输入后输入框、复选框和下拉均保持可用。
- 真实火山引擎 `ark-code-latest` 短调用返回“LLM回归测试通过”。
- 390x844 窄屏下页面宽度 390、无横向溢出，交互控件没有文本溢出。

## 永久回归门禁

后续修改科目、笔记同步、策略偏好、LLM 设置、LLM 服务或生成任务时，必须重复以下检查：

1. 连续创建、已有后缀和重命名冲突均必须得到无冲突后缀；只编辑其他字段不得改名；测试只能清理自身创建的临时数据。
2. 科目卡片计数必须与 `/api/notes?subject_id=...` 及进入后的列表一致，不得恢复跨页面陈旧计数的 `Math.max` 合并。
3. 产品级 Skill 必须来自已安装 Skill 搜索结果，不得退回任意关键词写入。
4. 环境变量必须使用完整原生下拉；手动输入模式不得因空值自动折叠。
5. LLM HTTP 成功但正文为空时只重试一次；不得把空正文当成功，也不得无限重试。
6. 必须运行后端全套、前端回归、生产构建，并对上述界面做真实浏览器抽查。
