# 2026-08-07 七项修改交付记录

本文记录本次本地演示版交付的功能组合、验证方法与外部产物，用于按照《仓库级别开发宪法准则》复现和回滚。

## 1. 功能清单与 Git 事实源

| 需求 | 功能分支 | 合并提交 | 交付结果 |
|---|---|---|---|
| 任务跳转到产品库并追溯全部产品；产品、任务、科目时间戳 | `feature/18-task-product-traceability` | `16383e3` | 任务按钮使用 `taskId` 筛选产品库；新产品保存任务编号；历史产品标记为未关联 |
| 面试题等产品不泄漏模型思考过程 | `feature/08-reasoning-leak-fix` | 对应功能提交 `534651e`、`f81c743`、`5543148`、`c9e6b97` | 生成、抛光和最终交付阶段均清洗推理标签与提示词泄漏 |
| Skills、算法、质量技术兼容检查与 WebUI 说明 | `feature/11-strategy-compat`、`feature/20-strategy-ui-skill-pack` | `674b3eb`、`0a803ce` | 后端唯一注册表；WebUI 显示可运行状态和降级警告 |
| MemoryBear 与 RAG 自由组合说明 | `feature/13-algo-docs` | `95a22f2` | 提供职责、路由、并行组合、替代与降级边界文档 |
| 下架 8 种产品类型，保留 14 种并映射 Skills | `feature/12-product-type-removal`、`feature/14-skill-mapping` | `90ff723`、`f511021` | API 和 WebUI 仅暴露 14 种；每种至少映射一个真实 Skill |
| PPT 产品统一命名 | `feature/09-product-type-cleanup`、`feature/20-strategy-ui-skill-pack` | `de0ce48`、`0a803ce` | 产品键为 `ppt`，对外名称为 `PPT` |
| 本地 LLM 配置从环境变量导入 Key | `feature/10-llm-env-import` | `e3b0103` | 仅本地模式显示环境变量选项；云端不暴露；不返回变量值 |
| 一键启动稳定性 | `fix/17-launcher-health-gate` | `dad4252` | 启动前校验依赖与前端资产，健康检查通过后才报告成功 |
| 策略可无 MemoryBear 独立运行 | `fix/19-strategy-free-combination` | `75620bc` | 任意合法技术组合不再因变量初始化位置而失败 |

## 2. 算法文档

- `docs/自由组合策略原理.md`：说明三轴正交模型、登记表、咨询式兼容校验、执行顺序和降级语义。
- `docs/MemoryBear与RAG混合检索架构.md`：说明 MemoryBear、源笔记约束和 RAG 的职责边界；明确当前真实 RAG 尚未接入向量库。
- `docs/产品类型技能映射表.md`：列出 14 种产品与真实 Skills 的逐项映射及选择理由。

## 3. 外部交付产物

- 文件：`Learn2Earn_14种产品生成Skills包.zip`
- 内容：当前 14 种产品所需的去重 Skills、`README.md` 和产品类型映射清单。
- 大小：2,561,854 bytes。
- SHA-256：`9C15480A63EF79A05EF8A1EF537B3249B30541DEB132E04B342839061B9167DD`
- 复现：`python scripts/build_product_skill_pack.py <输出ZIP路径>`。

该 ZIP 属于交付产物，不进入 Git；Git 只保存可复现它的脚本和映射。

## 4. 验证记录

```powershell
python -m pytest backend/tests -q
# 331 passed

cd frontend
npm test
# 3 passed（任务跳转、按任务筛选、任务标签）

npm run build
# vite production build succeeded
```

专项覆盖包括：任务到产品的 `task_id` 关联、仅源笔记技术组合、推理泄漏端到端清洗、14 种产品下架集合、Skills 映射完整性、精简 ZIP 内容、MemoryBear/RAG 状态及本地环境变量 Key 注入。

一键启动脚本实测启动成功，运行地址由脚本在 `9000..9010` 中选择空闲端口；本次验收为 `http://127.0.0.1:9000/`。运行态 API 返回 14 种产品、`PPT` 名称、MemoryBear `implemented=true`、RAG `implemented=false`。GUI 已核对科目创建时间、生成策略组合检查、MemoryBear/RAG 说明和 14 种产品选择项。

## 5. 安全与边界

- API Key 不写入源码、Git 或交付文档；环境变量接口只返回允许选择的变量名和是否可用，不返回值。
- RAG 当前是显式占位能力，不宣称已有真实向量检索；MemoryBear 和源笔记约束可独立完成当前生成增强。
- 用户原有的 `修改意见08042.md` 修改和 `Super Memory技术调研1785853322799.pdf` 不属于本次代码提交。
